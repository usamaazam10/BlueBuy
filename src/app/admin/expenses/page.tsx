'use client';

import { ExpensesBrowser } from '@/components/admin/business/finance-browsers';

/** Operating expenses — the cost side of operating profit. */
export default function ExpensesPage() {
  return <ExpensesBrowser />;
}
