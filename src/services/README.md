# services

External data-access and integration layer: API clients, Firebase service
wrappers, and third-party SDK adapters. Keep network/IO concerns here so that
`features/` and `components/` stay focused on UI and orchestration.
